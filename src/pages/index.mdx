import { format } from "date-fns"
import { mix } from "@theme-ui/color"

<SiteMetaData>
{siteMetadata => {
  const { name, description } = siteMetadata
  return (
        <Fragment>
        <Heading variant='styles.h1'>{name}</Heading>
        <Text>{description}</Text>
        </Fragment>
  )
}}
</SiteMetaData>

<Divider/>

<Button sx={{mb: 2}} as='a' variant='success' href='https://austriker27.github.io/resume/' target='_blank'>View Resume</Button>{" "}
<Divider />

<Divider />

## About

I love building stuff online. Like (nearly) all millennial developers I started off my dev career many, many moons ago playing with Legos. Since then I've been hooked on building things from start to finish. This innate curiosity and love for technology led me to world of development. I especially love crafting intuitive digital solutions for compelling brands and creating beautiful user experiences that surprise and delight end users.

<Divider />
<Divider />

<Flex sx={{ flexWrap: "wrap" }}>

  <Box
    sx={{
      width: ["100%", "100%", "55%"],
      mb: 3,
      pr: [0,0, 4],
    }}
  >

## [Contact](#contact)


  <Heading variant='styles.h3' sx={{mb: 0}}>Email</Heading>
  <Link href='mailto:davidalindahl@gmail.com' variant='styles.a'>*************@gmail.com</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0,}}>Twitter</Heading>
  <Link href='https://twitter.com/austriker27/' variant='styles.a'>https://twitter.com/austriker27/</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0}}>GitHub</Heading>
  <Link href='https://github.com/austriker27/' variant='styles.a'>https://github.com/austriker27/</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0}}>LinkedIn</Heading>
  <Link href='https://www.linkedin.com/in/davidalindahl/' variant='styles.a'>https://linkedin.com/in/davidalindahl/</Link>
  <Divider />

  </Box>

  <Box
    sx={{
      width: ["100%", "100%", "45%"],
      mb: 3
    }}
  >

## [things I use often](#tech-stack)

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("muted", "primary", `${index / tags.length}`),
                borderColor: mix("muted", "primary", `${index / tags.length}`),
              }}
            >
              {name}
            </Badge>
          )
        })}
    </Box>
  )}
</SourceTags>

  </Box>

</Flex>

<Divider />

## [Portfolio](#portfolio)

Examples of my coding prowess

<SourceList filter="portfolio">
  {(projects) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {projects
        .sort(
          (a, b) =>
            new Date(b.node.frontmatter.date) -
            new Date(a.node.frontmatter.date)
        )
        .map((edge, index) => {
          const {
            frontmatter: { date, dateModified, author, title, tags },
            fields: { slug },
            excerpt,
          } = edge.node
          return (
            <Box
              key={index}
              sx={{
                display: "flex",
                flex: "1 1 auto",
                flexDirection: "column",
                mb: 3,
                width: ["100%", "50%"],
                maxWidth: ["100%", "50%"],
              }}
            >
              <Card
                sx={{
                  display: "flex",
                  flex: "1 1 auto",
                  flexDirection: "column",
                  minHeight: "1px",
                  ml: 2,
                  mr: 2,
                  p: 3,
                }}
              >
                <Box
                  sx={{
                    display: "flex",
                    flex: "1 1 auto",
                    flexDirection: "column",
                  }}
                >
                  <Flex>
                    <Text variant="styles.small">
                      {format(new Date(date), "u")}
                    </Text>
                    <Text sx={{ mx: 1 }} variant="styles.small">
                      -
                    </Text>
                    {dateModified && (
                      <Text variant="styles.small">
                        {format(new Date(dateModified), "u")}
                      </Text>
                    )}
                  </Flex>
                  <Heading variant="styles.h4" sx={{ color: "text", mt: 1 }}>
                    {title}
                  </Heading>
                  <Link
                    sx={{ fontSize: 0, mb: 3 }}
                    href={author}
                    target="_blank"
                  >
                    {author}
                  </Link>
                  <Text
                    sx={{
                      fontSize: 0,
                      color: "text",
                      mb: 3,
                      wordBreak: "break-word",
                    }}
                  >
                    {excerpt}
                  </Text>
                </Box>
                <Box>
                  {tags.map((tag, index) => {
                    return (
                      <Badge
                        key={index}
                        variant="primary"
                        sx={{
                          mr: 2,
                          mb: 2,
                          color: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                          borderColor: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                        }}
                      >
                        {tag}
                      </Badge>
                    )
                  })}
                </Box>
              </Card>
            </Box>
          )
        })}
    </Flex>
  )}
</SourceList>

<Divider />
<Divider />


## [Side Projects](#side-projects)

I love building things and helping people.

<SourceList filter="sideprojects">
  {(projects) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {projects
        .sort(
          (a, b) =>
            new Date(b.node.frontmatter.date) -
            new Date(a.node.frontmatter.date)
        )
        .map((edge, index) => {
          const {
            frontmatter: { date, title, tags, author },
            fields: { slug },
            excerpt,
          } = edge.node
          return (
            <Box
              key={index}
              sx={{
                display: "flex",
                flex: "1 1 auto",
                flexDirection: "column",
                mb: 3,
                width: ["100%", "50%"],
                maxWidth: ["100%", "50%"],
              }}
            >
              <Card
                sx={{
                  display: "flex",
                  flex: "1 1 auto",
                  flexDirection: "column",
                  minHeight: "1px",
                  ml: 2,
                  mr: 2,
                  p: 3,
                }}
              >
                <Box
                  sx={{
                    display: "flex",
                    flex: "1 1 auto",
                    flexDirection: "column",
                  }}
                >
                  <Text variant="styles.small">
                    {format(new Date(date), "d-MMM-u")}
                  </Text>
                  <Heading variant="styles.h4" sx={{ color: "text", mt: 1 }}>
                    {title}
                  </Heading>
                  <Text
                    sx={{
                      fontSize: 0,
                      color: "text",
                      mb: 3,
                      wordBreak: "break-word",
                    }}
                  >
                    {excerpt}
                  </Text>
                  <Link
                    sx={{ fontSize: 0, mb: 3 }}
                    href={author}
                    target="_blank"
                  >
                    { author }
                  </Link>
                </Box>
                <Box>
                  {tags.map((tag, index) => {
                    return (
                      <Badge
                        key={index}
                        variant="primary"
                        sx={{
                          mr: 2,
                          mb: 2,
                          color: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                          borderColor: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                        }}
                      >
                        {tag}
                      </Badge>
                    )
                  })}
                </Box>
              </Card>
            </Box>
          )
        })}
    </Flex>
  )}
</SourceList>
<Divider />
<Divider />

<Flex sx={{ flexWrap: "wrap" }}>  
  <Box
    sx={{
      width: ["100%", "50%", "33.333%"]
    }}
  >          

  ## Experience
  <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://indigoslate.com"
    target="_blank"
    >
    indigo slate
    </Link>
  <Text variant='styles.medium'>user interface developer</Text>
  <Text variant='styles.small'>Sept 2018 - Present</Text>
  <Divider />

  <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://rainierwatch.com"
    target="_blank"
    >
    rainier watch
  </Link>
  <Text variant='styles.medium'>founder, designer, marketer, developer</Text>
  <Text variant='styles.small'>2013 - Present</Text>
  <Divider />

  <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://lindahlstudios.com"
    target="_blank"
    >
    lindahl studios
  </Link>
  <Text variant='styles.medium'>freelance front-end developer</Text>
  <Text variant='styles.small'>Sept 2017 - Present</Text>
  <Divider />

  <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://davidlindahlphoto.com"
    target="_blank"
    >
    david lindahl photography
  </Link>
  <Text variant='styles.medium'>freelance photographer</Text>
  <Text variant='styles.small'>March 2010 - Present</Text>
  <Divider />

  <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://capitalone.com"
    target="_blank"
    >
    capital one investing
  </Link>
  <Text variant='styles.medium'>anti-money laundering investigator</Text>
  <Text variant='styles.small'>2013 - 2017</Text>
  <Divider />
  <Divider />
  </Box>

  <Box
    sx={{
      width: ["100%", "50%", "33%"]
    }}
  >       

  ## Presentations  

  <Text>Mapbox & Vue.js</Text>
  <Text variant='styles.small'>Mar 2020</Text>
  <Divider />

  <Text>Vue.js Tips</Text>
  <Text variant='styles.small'>April 2019</Text>
  <Divider />

  <Text>Design Tips for Non Designers</Text>
  <Text variant='styles.small'>Oct 2018</Text>
  <Divider />
  <Divider />
  </Box>

  <Box
    sx={{
      width: ["100%", "50%", "33%"]
    }}
  >          

  ## Accolades

  <Text>Top 1% of global listeners to Johnny Cash (Spotify)</Text>
  <Text variant='styles.small'>Mar 2020</Text>
  <Divider />

  <Text>2020 Outstanding Contributor UI Team</Text>
  <Text variant='styles.small'>Jan 2020</Text>
  <Divider />

  <Text>"Biggest Techie": Operations Department</Text>
  <Text variant='styles.small'>Mar 2016</Text>
  <Divider />

  <Text>1st Place: Global Employee Photo Contest (Wildlife Category)</Text>
  <Text variant='styles.small'>Jun 2013</Text>
  <Divider />
  <Divider />
  </Box>

</Flex>

## This site

<Text variant='styles.small'>made with <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://www.gatsbyjs.org/"
    target="_blank"
    >gatsby</Link> and deployed on <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://www.netlify.com/"
    target="_blank"
    >netlify</Link>     |     <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://github.com/PaulieScanlon/gatsby-theme-terminal"
    target="_blank"
    >gatsby terminal theme</Link> by <Link
    sx={{ mb: 3, color: 'text' }}
    href="https://twitter.com/PaulieScanlon"
    target="_blank"
    >paul scanlon</Link>
    </Text>

<!-- made with [gatsby](https://www.gatsbyjs.org/) and deployed on [netlify](https://www.netlify.com/). [gatsby terminal theme](https://github.com/PaulieScanlon/gatsby-theme-terminal) by [paul scanlon](https://twitter.com/PaulieScanlon) -->

<Divider />
<Divider />
